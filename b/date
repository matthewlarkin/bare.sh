#!/usr/bin/env bash
source "$(dirname "${BASH_SOURCE[0]}")/../lib/init"


command="$1" && shift

case $command in

	cal )

		cal "$@" ;;

	# date, January 1, 1970 style
	date )
		;;

	stamp )
		;;

	countdown ) 
		;;

	diff )

		;;

	format ) 

		;;
esac