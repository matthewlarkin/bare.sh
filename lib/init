#!/bin/bash
set -e

cd "$(dirname "${BASH_SOURCE[0]}")/../"

source lib/colors
source lib/deps

# Load the env variables
[[ ! -f lib/.env ]] && cp lib/samples/.env lib/.env; source lib/.env && export NB_DIR="$BARE_NOTES_DIR"