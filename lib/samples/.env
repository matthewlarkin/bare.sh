#!/bin/bash

export BARE_OS="${BARE_OS:-$(uname -s)}"
export BARE_DIR="${BARE_DIR:-$(pwd)}"
export BARE_ENCRYPTION_KEY="${BARE_ENCRYPTION_KEY:-}"
export GIT_NAME="${GIT_NAME:-}"
export GIT_EMAIL="${GIT_EMAIL:-}"
export OPENAI_API_KEY="${OPENAI_API_KEY:-}" # (AI)
export POSTMARK_SERVER_TOKEN="${POSTMARK_SERVER_TOKEN:-}" # (email)
export STRIPE_SECRET_KEY="${STRIPE_SECRET_KEY:-}" # (payments)
export DAILY_API_KEY="${DAILY_API_KEY:-}" # (video conferencing)
export BARE_NOTES_DIR="${BARE_NOTES_DIR:-$BARE_DIR/var/.nb}"