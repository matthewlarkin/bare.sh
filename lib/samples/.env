#!/bin/bash

export BARE_OS="$(uname -s)"
export BARE_DIR="$(pwd)"
export BARE_ENCRYPTION_KEY=""
export OPENAI_API_KEY="" # (AI)
export POSTMARK_SERVER_TOKEN="" # (email)
export DAILY_API_KEY="" # (video conferencing)
export NB_DIR="$BARE_DIR/var/.nb"