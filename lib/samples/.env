#!/usr/bin/env bash

export BARE_OS="${BARE_OS:-$(uname -s)}"
export BARE_DIR="${BARE_DIR:-$(pwd)}"
export BARE_ENCRYPTION_KEY="${BARE_ENCRYPTION_KEY:-}"
export GIT_NAME="${GIT_NAME:-}"
export GIT_EMAIL="${GIT_EMAIL:-}"
export BARE_OPENAI_API_KEY="${BARE_OPENAI_API_KEY:-}" # (AI)
export BARE_POSTMARK_SERVER_TOKEN="${BARE_POSTMARK_SERVER_TOKEN:-}" # (email)
export BARE_STRIPE_SECRET_KEY="${BARE_STRIPE_SECRET_KEY:-}" # (payments)
export BARE_DAILY_API_KEY="${BARE_DAILY_API_KEY:-}" # (video conferencing)
export BARE_NOTES_DIR="${BARE_NOTES_DIR:-$BARE_DIR/var/.nb}"
export BARE_EDITOR="vim"