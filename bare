#!/usr/bin/env bash
set +H
set +o histexpand
source "$(dirname "${BASH_SOURCE[0]}")/lib/init"


[[ -z $1 ]] && echo "No scope specified" && exit 1

[[ -e "b/$1" ]] && {
	scope="$1" && shift
	"b/$scope" "$@"
	exit 0
}

echo "$@"